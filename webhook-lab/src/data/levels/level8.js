export const level8 = {
  id: "level-8",
  title: "Level 8 – Webhook Anatomy",
  type: "theory",
  content: `
## Learning Objectives
By the end of this level, you will understand the structural components of a webhook request, including the URL, headers, and the JSON payload.

## Prerequisites
- Level 7 (Intro to Webhooks)

## Concept Explanation
A webhook request looks exactly like a standard HTTP POST request. However, because it is automatically generated by a provider, you need to understand how the provider constructs it.

The anatomy of a webhook consists of:
1. **The Endpoint URL**: A publicly accessible URL on your server where the provider sends the data (e.g., \`https://api.myapp.com/webhooks/stripe\`).
2. **The Headers**: Metadata sent by the provider. Crucially, this usually includes a custom signature header (e.g., \`Stripe-Signature\`) to prove the request is authentic.
3. **The Payload**: A JSON body containing the actual event data. It usually has an \`event_type\` or \`action\` field indicating what happened.

## Real-World Analogy
Think of a webhook like receiving a certified package in the mail.
- **The Endpoint URL**: Your home address.
- **The Headers**: The certified stamp and sender's return address proving it's really from the bank.
- **The Payload**: The actual contents inside the box.

## Visual Diagram
\`\`\`mermaid
classDiagram
    class WebhookRequest {
        +String Method: POST
        +String URL: /api/webhook
        +Object Headers: Signature, Content-Type
        +Object Body: JSON Payload
    }
\`\`\`

## Technical Deep Dive: Event Payloads
Unlike REST APIs where you explicitly ask for a specific user object, webhook payloads are often "fat" or "thin".
- **Fat Payload**: Contains the entire object that was updated. (e.g., The entire user profile). This saves you from making a follow-up API call.
- **Thin Payload**: Only contains the ID of the object that changed. You must immediately make an API \`GET\` request to fetch the actual data. This is more secure for highly sensitive data.

## Code Example
This is what a raw webhook request coming from Stripe looks like:

\`\`\`http
POST /webhooks/stripe HTTP/1.1
Host: api.your-app.com
Content-Type: application/json
Stripe-Signature: t=161429...,v1=4d5e...

{
  "id": "evt_12345",
  "type": "payment_intent.succeeded",
  "data": {
    "object": {
      "id": "pi_123",
      "amount": 2000,
      "currency": "usd"
    }
  }
}
\`\`\`

## Common Mistakes
- **Assuming all providers use the same payload structure:** Every provider is different. GitHub uses \`action\`, Stripe uses \`type\`. You must read the specific documentation for each tool.
- **Ignoring Headers:** If you don't parse the headers, you cannot verify the webhook's authenticity later.

## Troubleshooting
- **Getting a 415 Unsupported Media Type?** Your server might be expecting \`application/x-www-form-urlencoded\` but the webhook is sending \`application/json\`.

## Best Practices
- **Use dedicated URLs:** Don't send Stripe, GitHub, and Slack webhooks to the same \`/webhook\` URL. Use \`/webhooks/stripe\` and \`/webhooks/github\` to keep your routing clean and separate.

## Hands-On Lab
*In a later level, you will spin up a real receiver. For now, study the JSON structure above to understand how events are classified.*

## Key Takeaways
1. Webhooks are just HTTP POST requests containing event data.
2. The JSON payload usually contains a specific "type" or "action" field.
3. Payloads can be "fat" (all data included) or "thin" (only IDs included).

## What's Next
Now that you know what a webhook looks like, it's time to understand the entire lifecycle of how it gets from the provider to your server.
`,
  quiz: {
    question: "What is the primary difference between a 'fat' and a 'thin' webhook payload?",
    options: [
      "Fat payloads use XML, thin payloads use JSON.",
      "Fat payloads contain the complete data object, while thin payloads only contain an ID, requiring a follow-up API call to fetch the data.",
      "Fat payloads are sent via POST, thin payloads are sent via GET.",
      "Fat payloads are encrypted, thin payloads are plain text."
    ],
    correctAnswerIndex: 1,
    explanation: "Thin payloads only give you the ID of what changed. This is highly secure but requires you to make an API call to get the rest of the information."
  }
};
